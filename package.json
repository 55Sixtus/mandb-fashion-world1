{
  "name": "ai-studio-applet",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run build && (npx serve -p 3000 .)",
    "build": "npx tailwindcss -i ./src.css -o ./styles.css --minify",
    "start": "npx serve -p 3000 .",
    "lint": "echo 'No lint'",
    "clean": "echo 'No clean'"
  },
  "devDependencies": {
    "serve": "^14.2.6",
    "tailwindcss": "^3.4.1"
  },
  "dependencies": {
    "html-minifier-terser": "^7.2.0"
  }
}
